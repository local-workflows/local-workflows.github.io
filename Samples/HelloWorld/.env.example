# A committed .env, so the sample works on a fresh clone.
#
# Values here are read literally - no ${{ }} resolution happens in a
# .env, because a $ or a {{ inside a token is a character, not a
# template.

DEMO_GREETING=hello from .env.example
DEMO_SUBDIR=scripts
