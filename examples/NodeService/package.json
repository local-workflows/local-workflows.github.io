{
    "name": "node-service-example",
    "version": "0.1.0",
    "private": true,
    "description": "A small todo list, used by the Local Workflows NodeService example.",
    "type": "module",
    "engines": {
        "node": ">=20"
    },
    "scripts": {
        "typecheck": "tsc --noEmit",
        "build": "tsc",
        "test": "vitest run",
        "test:watch": "vitest",
        "start": "node dist/cli.js"
    },
    "devDependencies": {
        "@types/node": "^22.10.2",
        "typescript": "^5.7.2",
        "vitest": "^2.1.8"
    }
}
